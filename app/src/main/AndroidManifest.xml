<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_foodie_icon"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_foodie_icon_round"
        android:supportsRtl="true"
        android:resizeableActivity="false"
        android:theme="@style/AppTheme"
        android:windowSoftInputMode="stateHidden"
        >

        <activity android:name="com.piyush.foodie.activity.OrderPlacedActivity"/>

        <activity android:name="com.piyush.foodie.activity.ProfileActivity"/>

        <activity android:name="com.piyush.foodie.activity.OrderHistoryActivity"/>

        <activity android:name="com.piyush.foodie.activity.CartActivity"/>

        <activity android:name="com.piyush.foodie.activity.RestaurantMenuActivity"/>

        <activity android:name="com.piyush.foodie.activity.Dashboard"/>

        <activity android:name="com.piyush.foodie.activity.LoginRegisterActivity" />

        <activity android:name="com.piyush.foodie.activity.SplashActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>